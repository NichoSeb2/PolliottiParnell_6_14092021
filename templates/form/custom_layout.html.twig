{% use 'bootstrap_5_layout.html.twig' %}

{%- block form_row -%}
	{% set row_attr = row_attr|merge({'class': (row_attr.class|default('') ~ ' my-3')|trim}) %}
    {{- parent() -}}
{%- endblock form_row -%}

{%- block form_label -%}
    {% set label_attr = label_attr|merge({'class': (label_attr.class|default('') ~ ' fs-5 m-0')|trim}) %}
    {{- parent() -}}
{%- endblock form_label %}